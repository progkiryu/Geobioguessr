# Loaded automatically by `vite` / `vite dev` (mode: development).
# Empty/'/api' routes requests through the dev proxy in vite.config.ts,
# which forwards /api -> http://localhost:4000 (avoids CORS in local dev).
VITE_API_URL=/api
