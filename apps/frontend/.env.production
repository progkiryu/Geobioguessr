# Loaded automatically by `vite build` (mode: production), incl. the GitHub
# Pages workflow. Must end in /api — the backend mounts all routes under /api.
VITE_API_URL=https://api.geobioguessr.com/api
